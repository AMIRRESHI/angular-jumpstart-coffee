import { CoffeeRoutingModule } from './coffee-routing-module.module';

describe('CoffeeRoutingModule', () => {
  it('should create an instance', () => {
    expect(new CoffeeRoutingModule()).toBeTruthy();
  });
});
